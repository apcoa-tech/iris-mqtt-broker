# Environment Configuration Template
# Copy this to env.dev, env.uat, env.prd and customize for each environment

# Environment name
ENVIRONMENT=dev

# Azure Resource Group
RESOURCE_GROUP=iot-dev

# Storage Account (where file shares are located)
STORAGE_ACCOUNT=irisstdev001

# Azure File Shares
FILE_SHARE_CONFIG=mqtt-config
FILE_SHARE_CERTS=mqtt-certs

# Azure Container Instance
CONTAINER_GROUP=iris-dev-mqtt
CONTAINER_NAME=mosquitto

# Secrets stored in GitHub Secrets (not Azure Key Vault)
# Required GitHub Secrets:
# - MQTT_BRIDGE_PASSWORD
# - MQTT_USER_DEVICEUSER_PASSWORD
# - MQTT_USER_ADMINUSER_PASSWORD

# Azure Container Registry
ACR_NAME=irisacr
ACR_LOGIN_SERVER=irisacr.azurecr.io
IMAGE_NAME=mosquitto
IMAGE_TAG=latest

# Bridge Configuration
BRIDGE_REMOTE_ADDRESS=3.121.198.76:8883
BRIDGE_USERNAME=logforwarder

# Local Users (usernames only, passwords in Key Vault)
LOCAL_USERS=deviceuser,adminuser
