# Development Environment Configuration
# DEV has DUAL MQTT brokers to simulate production setup
#
# Architecture:
# - mqtt-1: Simulates OUR production broker (bridges TO mqtt-2)
# - mqtt-2: Simulates EXTERNAL COMPANY's broker (standalone, receives from mqtt-1)
#
# This allows testing the bridge integration without access to external company's dev environment

ENVIRONMENT=dev
RESOURCE_GROUP=iot-dev
STORAGE_ACCOUNT=irisstdev001

# Dual broker configuration
DEPLOY_DUAL_BROKERS=true

# MQTT Broker 1 - Our Production Simulator
FILE_SHARE_CONFIG_1=iris-dev-mqtt-1-config
CONTAINER_GROUP_1=iris-dev-mqtt-1
MQTT_1_FQDN=iris-dev-mqtt-1.westeurope.azurecontainer.io

# MQTT Broker 2 - External Company Simulator
FILE_SHARE_CONFIG_2=iris-dev-mqtt-2-config
CONTAINER_GROUP_2=iris-dev-mqtt-2
MQTT_2_FQDN=iris-dev-mqtt-2.westeurope.azurecontainer.io

# Shared resources
FILE_SHARE_CERTS=iris-dev-mqtt-certs
CONTAINER_NAME=mosquitto

# Container Registry
ACR_NAME=irisacrdev001
ACR_LOGIN_SERVER=irisacrdev001.azurecr.io
IMAGE_NAME=mosquitto
IMAGE_TAG=latest

# Bridge Configuration
# mqtt-1 bridges TO mqtt-2 (simulating: our broker -> external company)
EXTERNAL_BROKER_ADDRESS=iris-dev-mqtt-2.westeurope.azurecontainer.io:8883
BRIDGE_USERNAME=external_integration_user

# Local Users
# mqtt-1: deviceuser (for IoT devices), adminuser (for monitoring)
# mqtt-2: external_integration_user (accepts bridge from mqtt-1)
LOCAL_USERS=deviceuser,adminuser,external_integration_user
